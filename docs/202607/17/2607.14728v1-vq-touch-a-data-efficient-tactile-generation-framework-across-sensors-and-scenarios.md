---
title: "VQ-Touch: A Data-Efficient Tactile Generation Framework Across Sensors and Scenarios"
title_zh: "VQ-Touch: 一种跨传感器和场景的数据高效触觉生成框架"
authors: "Kailin Lyu, Long Xiao, Jianing Zeng, Di Wu, Lin Shu, Jie Hao"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14728v1"
tags: ["query:tactile-vla"]
score: 8.0
evidence: 数据高效的跨传感器触觉生成框架
tldr: 触觉图像生成可降低对昂贵传感器的依赖。现有方法依赖大规模数据集且泛化能力弱。本文提出VQ-Touch框架，利用DM-VQGAN高效提取触觉特征，通过离散扩散解码器和统一条件接口支持多模态生成，并通过小样本混合训练增强跨传感器和场景的泛化能力。实验表明VQ-Touch在多任务上超越现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14728v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 920, \"height\": 503, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14728v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1689, \"height\": 771, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14728v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 922, \"height\": 359, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14728v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 909, \"height\": 644, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14728v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 922, \"height\": 205, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14728v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 916, \"height\": 287, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14728v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 918, \"height\": 219, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14728v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 908, \"height\": 213, \"label\": \"Table\"}]"
motivation: 现有触觉生成方法依赖大规模数据集且跨传感器泛化能力差，在视觉受限环境中表现不佳。
method: 提出VQ-Touch框架，包含DM-VQGAN触觉表示学习器和离散扩散解码器，支持图像和标签等多模态条件输入，并通过小样本混合训练实现跨传感器兼容。
result: 实验证明VQ-Touch在多个生成任务上超越当前最优方法。
conclusion: VQ-Touch是一种高效、跨传感器、多场景的触觉生成框架，可减少对昂贵传感器的依赖。
---

## 摘要
触觉图像生成通过合成高保真触觉数据，显著减少了对昂贵且易磨损传感器的依赖，为机器人感知和人机交互系统中的触觉信息获取提供了高效解决方案。然而，现有方法依赖于特定传感器的大规模、多样化数据集，且缺乏高效的数据利用和鲁棒的泛化能力，在视觉受限环境中表现不佳。为解决这一问题，我们提出了VQ-Touch，一种支持跨传感器和多场景应用的触觉生成框架。具体而言，为了高效提取数据中的复杂形变和纹理特征，我们提出了DM-VQGAN，一种有效的触觉表示学习器。此外，我们引入了一种具有统一条件接口的离散扩散解码器，支持图像和标签等多模态生成任务，并通过少样本混合训练增强了模型的泛化能力，从而实现了对当前主流传感器及其变体的兼容。实验表明，VQ-Touch在多个任务中超越了最先进的方法。

## Abstract
Tactile image generation significantly reduces the dependency on expensive and wear-prone sensors by synthesizing high-fidelity tactile data, offering an efficient solution for tactile information acquisition in robotic perception and human-machine interaction systems. However, existing methods depend on large-scale, diverse datasets from specific sensors and lack efficient data utilization and robust generalization capabilities, struggling in vision-limited environments. To address this, we introduce VQ-Touch, a tactile generation framework that supports both cross-sensor and multi-scenario applications. Specifically, to efficiently extract complex deformation and texture features from the data, we propose DM-VQGAN, an effective tactile representation learner. Furthermore, we introduce a discrete diffusion decoder with a unified conditioning interface, supporting multimodal generation tasks such as images and labels, and enhances the model's generalization capability through few-shot mixed training, thus achieving compatibility with current mainstream sensors and their variants. Experiments show that VQ-Touch surpasses state-of-the-art methods in multiple tasks.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究动机**：触觉数据对于机器人操作和感知至关重要，但真实触觉图像获取成本高、传感器易磨损，导致数据稀缺且含噪声。现有触觉生成方法依赖特定传感器的大规模预训练数据集，跨传感器泛化能力差，且在视觉受限环境下性能显著下降。
- **核心问题**：能否学习一种数据高效的触觉表示，使其能够跨传感器、跨场景支持触觉生成，从而在少量数据下实现高质量合成？
- **整体含义**：VQ-Touch 通过构建高效的触觉表示学习器（DM-VQGAN）和统一的条件生成框架，首次实现了从极小样本（如500张图像）到跨传感器族、多模态输入的触觉图像生成，显著降低了对昂贵传感器和大规模标注数据的依赖，为机器人和人机交互系统提供可扩展的触觉数据合成方案。

### 2. 方法论：核心思想与关键技术细节

- **核心思想**：利用离散码本（VQGAN）对触觉图像低方差、高几何模式的特点进行结构化表示；通过可变形卷积和多尺度融合增强对非刚性形变和多尺度纹理的捕捉；提出传感器家族聚类与少样本混合训练策略实现跨传感器泛化；基于离散扩散解码器实现多模态条件生成。
- **关键技术细节**：
  1. **DM-VQGAN（编码器-解码器+码本）**：
     - 编码器 `E` 将输入图像 `x` 映射为潜在特征 `ˆz`，通过最近邻搜索量化到码本 `Z` 中得到 `z_q`，解码器 `G` 重构图像 `ˆx`。
     - 引入**可变形卷积**（公式3）：自适应学习卷积核的空间偏移量 `∆p_k`，捕捉局部不规则变形。
     - **多尺度融合模块**（公式4）：使用并行空洞卷积（dilation rates 1,2,4），扩大感受野，同时提取宏观形变和微观纹理。
     - 损失函数：L1重建损失 + 码本损失 + 承诺损失 + 感知损失 + 对抗损失（公式2）。
  2. **少样本混合训练与传感器家族聚类**：
     - 分析发现传感器差异主要来自**内在属性**（设计、制造）和**环境因素**（光照、校准）。同一家族传感器（如 GelSight 各变体）共享内在属性。
     - 聚类方法：用 DM-VQGAN 提取不同传感器图像的特征，迭代计算每个簇的原型向量，定义簇间距离（公式5）。新传感器被分配到最近家族，仅需少量样本（如50张）与该家族数据（1:5比例）混合训练码本，避免全量重训。
  3. **多模态条件生成框架**：
     - 离散扩散解码器：在 DM-VQGAN 的离散潜在空间上训练，支持三种条件输入：
       - 视觉图像：通过冻结的 CLIP 图像编码器得到 `c_visual`。
       - 触觉图像：通过 DM-VQGAN 编码器映射到码本索引得到 `c_tactile`。
       - 语义标签：直接编码为条件特征 `c_label`。
     - 条件特征与噪声潜在表示通过 Transformer 模块交互，实现条件触觉图像生成。

### 3. 实验设计

- **数据集与传感器**：
  - 数据集：Touch and Go（GelSight）、FabricVST（GelSight Mini）、VisGel（GelSight）、HCT（DIGIT）。
  - 传感器类型：GelSight、GelSight Mini、DIGIT，覆盖三种主流 VBTS 传感器家族。
- **基准方法**：
  - 重建任务：MAE（ViT-Base）、VQGAN。
  - 生成任务：UniTouch、PixArt-α、TextToucher、GVST（仅部分指标）、前述 MAE/VQGAN。
- **评估指标**：FID、SSIM、LPIPS、PSNR、Top-1 准确率、精确率、召回率、F1 分数。
- **主要实验设置**：
  - 表 I：不同训练样本量（100/1K/10K）下重建质量对比。
  - 表 II：视觉条件生成（HCT 和 SSVTP 数据集）的定量指标。
  - 表 III：生成图像用于下游物体识别任务的分类准确率（TAG 和 FabricVST 数据集）。
  - 表 IV：少样本（500/2K）重建对比（UniTouch vs Ours）。
  - 图 3：潜在空间可视化（DM-VQGAN vs CNN）和零样本重建示例。
  - 图 4：视觉/标签条件生成示例。
  - 图 5：跨传感器族重建（GelSight 变体和 DIGIT）的可视化结果。

### 4. 资源与算力

论文未明确说明训练所用的 GPU 型号、数量、训练时长等硬件资源与算力开销。仅在方法论部分提到模型架构（如编码器、解码器、码本、Transformer等），但未给出具体参数量和计算成本。

### 5. 实验数量与充分性

- **实验数量**：共 5 个主要表格（表I–IV）和 4 幅关键结果图（图3–5），涵盖：
  - 重建实验（不同样本量、不同方法）。
  - 多模态生成实验（视觉、标签条件）。
  - 下游识别任务（两个数据集）。
  - 少样本跨传感器泛化实验（图5，仅定性对比）。
- **充分性分析**：
  - **客观公平**：所有实验重复3次取均值，对比了多个SOTA方法，指标选择全面（FID/SSIM/LPIPS/PSNR/分类精度）。
  - **覆盖范围**：覆盖了3种传感器家族、4个数据集，但传感器数量有限（每个家族仅1–2种具体型号），未包含更多非主流传感器。
  - **消融实验**：仅对比了 VQGAN vs DM-VQGAN（表I）和 MAE vs VQGAN vs DM-VQGAN，但未单独消融可变形卷积、多尺度融合模块的贡献，也未消融传感器家族聚类策略的有效性（仅有定性图5）。
  - 在少样本实验中仅与 UniTouch 对比（表IV），未与其他方法（如 MAE/VQGAN 在少样本下）对比，略显不足。
- **结论**：实验设计整体较为充分，定量定性结合，但消融深度和对比广度尚有提升空间。

### 6. 论文的主要结论与发现

- **DM-VQGAN 优于现有重建方法**：在相同样本量下（甚至仅100张），DM-VQGAN 的重建 FID/SSIM 均优于 MAE 和原始 VQGAN，且潜在表示更紧凑、聚焦触觉特征（图3）。
- **VQ-Touch 生成质量领先**：在视觉/标签条件生成任务中，VQ-Touch 在 LPIPS、PSNR、SSIM 等指标上全面超越 UniTouch、PixArt-α、TextToucher 等 SOTA 方法（表II）。
- **生成数据对下游有益**：使用 VQ-Touch 生成的触觉图像训练分类器，在 TAG 和 FabricVST 数据集上的 Top-1 准确率比 PixArt-α 生成图像高出约3–4%（表III）。
- **少样本跨传感器泛化可行**：仅需少量新传感器样本（如50张）与同家族数据混合训练，即可获得可用的跨传感器码本，实现有效重建（图5）。
- **统一条件框架灵活性**：支持视觉、标签、触觉三种条件输入，适应不同应用场景。

### 7. 优点

- **数据高效性**：在仅100张训练图像时仍能获得优于 MAE 10K 样本的重建质量（表I），少样本场景下优势显著。
- **跨传感器泛化**：提出传感器家族聚类和混合训练策略，避免了为每个新传感器重新收集大规模数据，实用性强。
- **多模态条件支持**：统一的离散扩散解码器可同时处理图像、标签、触觉输入，适应多样化下游任务。
- **针对触觉特点的专用设计**：可变形卷积和空洞卷积多尺度模块分别强化对非刚性形变和多尺度纹理的捕捉，弥补了通用 VQGAN 的不足。
- **实验验证充分**：涵盖重建、生成、下游识别三大任务，且与多个 SOTA 方法对比，结果具有可信度。

### 8. 不足与局限

- **计算开销未报告**：训练资源（GPU型号、数量、时间）完全未提及，不利于复现和实际应用评估。
- **消融实验不够深入**：未单独验证可变形卷积、多尺度融合、码本尺寸等每个组件的贡献，传感器家族聚类策略只有定性结果，缺乏消融定量对比。
- **传感器覆盖有限**：仅涉及三种传感器家族（GelSight、GelSight Mini、DIGIT），未评估对更多异构传感器（如基于光电、电容的触觉传感器）的泛化能力。
- **未在真实机器人任务中验证**：下游识别任务使用的是静态图像分类，未在操作任务（如抓取、材质辨识）中评估生成数据的实用性。
- **鲁棒性未知**：未测试在极端光照、遮挡、传感器老化等环境因素下的生成稳定性。
- **潜在偏差风险**：生成图像可能偏向训练数据中常见的纹理/形变模式，对罕见场景（如变形剧烈或未知材料）的真实感可能不足。
- **未讨论生成图像与真实图像的物理一致性**：仅用感知指标（FID/SSIM）评价，缺少物理仿真或力触觉一致性验证。

（完）
